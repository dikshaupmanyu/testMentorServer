<%- include partials/innerheader %>
 <!-- Page Content Start -->
<style type="text/css">
  .tips_bx_amount span span.color_sell::after {
    display: inline-block;
    margin-left: .390em;
    vertical-align: 0.100em;
    content: "";
    border-top: .4em solid;
    border-right: .3em solid transparent;
    border-bottom: 0;
    border-left: .3em solid transparent;
}

.tips_bx_head img{
    width:100%
}
.tips_bx_cmpny{
    border-bottom: 1px solid rgb(255 255 255 / 32%);
    padding:0px 0px 15px
}
/* .tips_bx_txt{
    border-bottom: 1px solid rgb(255 255 255 / 32%);
    padding:0px 0px 15px
} */

.tips_bx_social{
    margin: 0px 0px 20px
}
</style>

<div class="app_main">
  <%- include partials/innersidebar %>

  <div class="app_content">
    <div class="dsbrd_container">
      <div class="trade_dtl_wrapper dsbrd_center_wrapper">
        <div class="trade_dtl_head">
          <a href="/dashboard"><button class="btn back_btn"><i class="fas fa-arrow-left"></i></button></a>
         <!--  <button class="btn cmn_btn btn_small btn_primary">View AI Trend</button> -->
        </div>
        <div class="trade_dtl_box tips_bx buy_bg" id="idOfElement">
          <div class="tips_bx_head">
            <img id="imageDetails" src=""/>
          </div>
          
          <div class="tips_bx_cmpny">
           <h2 id="title"></h2>
          </div>
  
          <div class="tips_bx_txt">
            <h3 id="descriptionOne"></h3>
            <br>
            <p id="descriptionTwo"></p>
          </div>

          <div class="tips_bx_social" >
            <h3 id="mentorName"> </h3>
            <br>
            <h4 id="price"></h4>
          </div>
      </div>
    </div>
  </div>
  </div>

<%- include partials/innerfooter %>


<script type="text/javascript">
    var LoggedInId = "<%= userid %>";
    // console.log(LoggedInId);

    var tokens = "<%= tokens %>";
    // alert(tokens);

    var user_name = "<%= userName %>";

    var sDetail = "<%=tipsIds%>";

  console.log(sDetail);

    $.ajax({
      type: "POST",
      url:
        "https://apistest.tradetipsapp.com/api/serviceMentorSubscriptionPlan/getAllTypeServicesForUser",
      headers: {
        Authorization: "Bearer " + tokens,
      },
      data: {
        userId: sDetail,
      },
      success: function (data) {
        // console.log(data)
        var datak = JSON.stringify(data);
        var fdataResult = JSON.parse(datak);
        var dataResult = fdataResult.suggestedServiceSubscriptionPlans;
        dataResult = dataResult.split("_")[0];
        console.log(dataResult)


          document.getElementById("imageDetails").src = "https://apistest.tradetipsapp.com" +dataResult.imageDetails;
          document.getElementById("title").innerHTML = dataResult.title;
          document.getElementById("descriptionOne").innerHTML = dataResult.descriptionOne;
          document.getElementById("descriptionTwo").innerHTML = dataResult.descriptionTwo;
          document.getElementById("mentorName").innerHTML = "By :   "+ dataResult.mentorName;
          document.getElementById("price").innerHTML = "Price   " + " " + dataResult.price;
        // }



      }

    })
    
 </script>